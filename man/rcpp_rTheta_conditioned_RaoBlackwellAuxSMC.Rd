% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{rcpp_rTheta_conditioned_RaoBlackwellAuxSMC}
\alias{rcpp_rTheta_conditioned_RaoBlackwellAuxSMC}
\title{Draw a trajectory from the state-space posterior via a Sequential Monte Carlo sampler}
\usage{
rcpp_rTheta_conditioned_RaoBlackwellAuxSMC(
  y,
  delta,
  x,
  matFF,
  mu0,
  Sigma0,
  G,
  G_star,
  SigmaEps,
  SigmaEps_star,
  Tstar,
  nSim = 500L
)
}
\arguments{
\item{y}{Integer vector of length \eqn{T} containing the binary observations
(0 or 1); \code{NA} entries are treated as missing.}

\item{delta}{Numeric vector of length \eqn{q} of fixed regression coefficients for
the static covariate \code{x}, defining the scalar offset
\eqn{\mu^* = \mathbf{x}^\top \boldsymbol{\delta}}.}

\item{x}{Numeric vector of length \eqn{q} of static covariates entering the probit
mean through \eqn{\mu^*}.}

\item{matFF}{\eqn{T \times p} observation design matrix.}

\item{mu0}{Numeric vector of length \eqn{p}: prior mean of the initial state
\eqn{\mathbf{x}_0 \sim \mathcal{N}_p(\boldsymbol{\mu}_0,
  \boldsymbol{\Sigma}_0)}.}

\item{Sigma0}{\eqn{p \times p} symmetric positive-definite prior covariance of the
initial state.}

\item{G}{\eqn{p \times p} state transition matrix for the standard regime,
\eqn{\mathbf{x}_t = \mathbf{G}\,\mathbf{x}_{t-1} +
  \boldsymbol{\varepsilon}_t}, \eqn{\boldsymbol{\varepsilon}_t \sim
  \mathcal{N}_p(\mathbf{0}, \boldsymbol{\Sigma}_\varepsilon)}.}

\item{G_star}{\eqn{p \times p} state transition matrix for the alternative regime,
applied at the time points listed in \code{Tstar}.}

\item{SigmaEps}{\eqn{p \times p} state evolution covariance for the baseline regime.}

\item{SigmaEps_star}{\eqn{p \times p} state evolution covariance for the alternative regime.}

\item{Tstar}{Integer vector of time indices at which the alternative regime
transition (\code{G_star}, \code{SigmaEps_star}) is used instead of the
baseline.}

\item{nSim}{Number of SMC particles.  Larger values reduce Monte Carlo variance at
the cost of memory and time per step.
Default: \code{500}.}
}
\value{
A \eqn{T \times p} numeric matrix.  Row \eqn{t} contains the state vector
\eqn{\mathbf{x}_t} of the single trajectory drawn from
\eqn{p(\mathbf{x}_{1:T} \mid y_{1:T})}.
}
\description{
Implements an particle filter for a binary, probit-linked Dynamic Model (DLM) with two transition
regimes. After the forward pass, a single trajectory
\eqn{\{\mathbf{x}_t\}_{t=1}^{T}} is extracted by backward tracing through
the stored indices.
}
\examples{
\dontrun{
set.seed(14041995)
TT  <- 50
p   <- 2
y   <- rbinom(TT, 1, 0.6)
y[sample(TT, 5)] <- NA          # inject some missing observations

matFF   <- matrix(c(rep(1, TT), seq_len(TT)), nrow = TT)
mu0     <- rep(0, p)
Sigma0  <- diag(p)
G       <- diag(p) * 0.05
G_star  <- diag(p) * 0.80
SigE    <- diag(p) * 0.1
SigE_s  <- diag(p) * 1
delta   <- c(0.1, -0.1)
x       <- c(1, 0)
Tstar   <- c(10L, 30L)          # regime switches at t = 10 and t = 30

traj <- rcpp_rTheta_conditioned_RaoBlackwellAuxSMC(
  y          = as.integer(y),
  delta      = delta,
  x          = x,
  matFF      = matFF,
  mu0        = mu0,
  Sigma0     = Sigma0,
  G          = G,
  G_star     = G_star,
  SigmaEps   = SigE,
  SigmaEps_star = SigE_s,
  Tstar      = Tstar,
  nSim       = 500L
)
dim(traj)    # TT x p
}

}
\seealso{
\code{\link{cpp_mvrnormArma_transposed}}, \code{\link{rcpp_rtnorm_vec}},
\code{\link{rcpp_sample}}, \code{\link{rcpp_index_gen}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{sample_biniary_ts_wSplit_from_G}
\alias{sample_biniary_ts_wSplit_from_G}
\title{Sample from the prior predictive distribution a realization for the state-space parameters.}
\usage{
sample_biniary_ts_wSplit_from_G(
  mu,
  TT,
  matFF,
  mu0,
  Sigma0,
  G,
  G_star,
  SigmaEps,
  SigmaEps_star,
  Tstar
)
}
\arguments{
\item{TT}{Length of the time series \eqn{T_i}.}

\item{matFF}{\eqn{T_i \times p} observation design matrix (same role as
\code{Zi} in \code{\link{rcpp_get_Ri_wSplit}}).}

\item{mu0}{Prior mean of the initial state.}

\item{Sigma0}{Prior covariance of \eqn{\mathbf{x}_0} (\eqn{p \times p}).}

\item{G}{Baseline transition matrix (\eqn{p \times p}).}

\item{G_star}{Alternative-regime transition matrix (\eqn{p \times p}).}

\item{SigmaEps}{Baseline evolution covariance (\eqn{p \times p}).}

\item{SigmaEps_star}{Alternative evolution covariance
(\eqn{p \times p}).}

\item{Tstar}{Numeric vector of \strong{0-based} split-point indices
at which the alternative regime is applied.}

\item{nSim}{Number of Monte Carlo draws.  Default: \code{10000}.}
}
\value{
A numeric vector of length \code{nSim} with values in
\eqn{\{0, \ldots, 7\}}, representing independent draws from the prior
predictive distribution of \eqn{R_i} under \eqn{g}.
}
\description{
Used to approximates the prior predictive distribution of the \eqn{R_i} by Monte Carlo simulation
under the state-space prior \eqn{g} defined in the manuscript, Section 3.  For each of \code{nSim}
draws:
\enumerate{
\item a full state trajectory
\eqn{\mathbf{x}_0, \mathbf{x}_1, \ldots, \mathbf{x}_T} is
simulated from the prior;
\item the risk index \eqn{R_i \in \{0, \ldots, 7\}} is computed via
\code{\link{rcpp_get_Ri_wSplit}}.
}
The returned vector of \code{nSim} draws can be used to estimate
\eqn{p(R_i = r \mid g)} for each \eqn{r}, enabling prior elicitation
and sensitivity analysis as described in the paper.
}
\examples{
\dontrun{
draws <- sample_Ri_under_g_wSplit(
  TT          = 365L,
  matFF       = matFF,
  mu0         = rep(0, p),
  Sigma0      = diag(p),
  G           = G,
  G_star      = G_star,
  SigmaEps    = SigE,
  SigmaEps_star = SigE_s,
  Tstar       = c(90, 365),
  nSim        = 10000L
)
}
}
\seealso{
\code{\link{rcpp_get_Ri_wSplit}},
\code{\link{rcpp_get_Ri_wSplit_from_eta}}
}
